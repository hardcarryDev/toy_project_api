generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("PRISMA_DB_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user {
  id            Int       @id
  user_id       String    @db.VarChar(20)
  user_password String    @db.VarChar(20)
  age           Int?
  sex           user_sex?
  created_at    DateTime
  updated_at    DateTime  @db.DateTime(0)
  posts         posts[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model comments {
  id         Int      @id
  post_id    Int?
  user_id    Int?
  body       String   @db.Text
  like       Int
  created_at DateTime
  updated_at DateTime @db.DateTime(0)
  posts      posts?   @relation(fields: [post_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "comments_FK")

  @@index([post_id], map: "FK_posts_TO_comments_1")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model posts {
  id         Int        @id
  title      String     @db.VarChar(255)
  body       String     @db.Text
  user_id    Int?
  created_at DateTime
  updated_at DateTime   @db.DateTime(0)
  comments   comments[]
  user       user?      @relation(fields: [user_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "posts_FK")

  @@index([user_id], map: "FK_user_TO_posts_1")
}

enum user_sex {
  M
  W
}
